# Third-party notices

The TypeScript implementation in this package is original and has zero runtime
dependencies.

The user-supplied `CAR-D-B005-1` PDF and associated PNG are visual references
only and are not embedded in the npm package. The package does include
self-contained generated polygon traces of the ECI logo and ornate north arrow
derived from user-supplied reference artwork. These traces are not copied CAD
source blocks. The downstream user must be authorized to use the ECI mark.

The package also includes a compact, generated 2D trace derived from visible
bound existing-substation geometry in a user-supplied Carousel DWG. It is not
the source DWG or original unbound xref. Downstream users must be authorized to
use and redistribute that project geometry.

Product names and drawing conventions are used descriptively for integration
and interoperability. The integrating organization is responsible for
authorization to use project names, customer branding, title-block content,
and issued-drawing metadata in production deliverables.